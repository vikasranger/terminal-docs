import MasterChip from "../../../../../components/MasterChip"

# <MasterChip value={"DELETE"} /> groupExit

## API Details

- **Description** Exit caller from the given group
- **Version** `v1`
- **Allowed Connection Types** `rpc` `wsoc` 
- **Allowed Deployment Kinds** `beta` `prod` `localHost` `test` 
- **Allowed Device Types** `test` `ios` `android` `web` `agent` 
- **SLA** 600
- **Token** bearer
- **Flags** Log message
- **Publishes** `groupMembership` `caller` `userCommonGroups` 
- **Scope** global


## Request

### Message format

```json
{
  "groupId": "GroupId",
  "version?": "string"
}
```

### Signal format

```json
{}
```

