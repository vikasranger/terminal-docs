import MasterChip from "../../../../../components/MasterChip"

# <MasterChip value={"GET"} /> groupCandidateListGet

## API Details

- **Description** Get candidate list for new group formation/adding group member to existing group
- **Version** `v1`
- **Allowed Connection Types** `rpc` `wsoc` 
- **Allowed Deployment Kinds** `beta` `prod` `localHost` `test` 
- **Allowed Device Types** `test` `ios` `android` `web` `agent` 
- **SLA** 600
- **Token** bearer
- **Flags** Log message
- **Scope** global


## Request

### Message format

```json
{
  "groupId?": "GroupId"
}
```

### Signal format

```json
{
  "candidateMap": {
    "AnyPrefixKey": [
      {
        "entId": "EntId",
        "entUserId": "EntUserId",
        "firstName": "string",
        "lastName": "string",
        "nickName?": "string",
        "userColor": "string",
        "handle?": "string",
        "about?": "string",
        "mediaIdAvatar?": "MediaIdAvatar",
        "isDeleted?": "boolean",
        "version": "string"
      }
    ]
  }
}
```

