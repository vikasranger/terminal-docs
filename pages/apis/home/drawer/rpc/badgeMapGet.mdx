import MasterChip from "../../../../../components/MasterChip"

# <MasterChip value={"GET"} /> badgeMapGet

## API Details

- **Description** Get badge map for caller
- **Version** `v1`
- **Allowed Connection Types** `wsoc` `rpc` 
- **Allowed Deployment Kinds** `beta` `prod` `localHost` `test` 
- **Allowed Device Types** `test` `ios` `android` `web` `agent` 
- **SLA** 600
- **Token** bearer
- **Flags** Log message
- **Scope** global


## Request

### Message format

```json
{
  "version?": "string"
}
```

### Signal format

```json
{
  "entChatBadgeMap": {
    "EntId": {
      "chatBadgeMap?": {
        "ChatId": "number"
      }
    }
  },
  "version": "string"
}
```

