import MasterChip from "../../../../../components/MasterChip"

# <MasterChip value={"PATCH"} /> callerPasswordReset

## API Details

- **Description** Reset password of the caller
- **Version** `v1`
- **Allowed Connection Types** `rpc` 
- **Allowed Deployment Kinds** `beta` `prod` `localHost` `test` 
- **Allowed Device Types** `test` `ios` `android` `web` `agent` 
- **SLA** 600
- **Token** refresh
- **Scope** global


## Request

### Message format

```json
{
  "currPassword": "string",
  "newPassword": "string"
}
```

### Signal format

```json
{}
```

