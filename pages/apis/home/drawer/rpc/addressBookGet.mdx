# addressBookGet

## API Details

- **Description** Get caller's address book
- **Version** `v1`
- **Method** `get`
- **Allowed Connection Types** `rpc` `wsoc` 
- **Allowed Deployment Kinds** `beta` `prod` `localHost` `test` 
- **Allowed Device Types** `test` `ios` `android` `web` `agent` 
- **SLA** 600
- **Token** bearer
- **Flags** Log message
- **Scope** global


## Request

### Message format

```json
{
  "version?": "string"
}
```

### Signal format

```json
{
  "candidateMap": {
    "AnyPrefixKey": [
      {
        "entUserId": "EntUserId",
        "handle": "string",
        "nickName": "string",
        "mediaIdAvatar?": "MediaIdAvatar"
      }
    ]
  },
  "othersList": [
    {
      "handle": "string",
      "nickName": "string"
    }
  ],
  "version": "string"
}
```

