import MasterChip from "../../../../../components/MasterChip"

# <MasterChip value={"GET"} /> lastMessageGet

## API Details

- **Description** Get last message for given chat
- **Version** `v1`
- **Allowed Connection Types** `wsoc` `rpc` 
- **Allowed Deployment Kinds** `beta` `prod` `localHost` `test` 
- **Allowed Device Types** `test` `ios` `android` `web` `agent` 
- **SLA** 600
- **Token** bearer
- **Flags** Log message
- **Scope** globalOrEnt


## Request

### Message format

```json
{
  "chatId": "ChatId",
  "messageOffset?": "number",
  "version?": "string"
}
```

### Signal format

```json
{
  "entId": "EntId",
  "chatId": "ChatId",
  "messageId": "MessageId",
  "messageOffset": "number",
  "senderId": "EntUserId",
  "receiptStatus?": "error | singleTick | doubleTick | blueTick",
  "messageType": "audio | camera | document | spreadsheetRow | spreadsheetRowDeleted | group | groupAboutChange | groupAvatarChange | groupCreate | groupExit | groupJoinWithInvite | groupMemberAdd | groupMemberRemove | groupNameChange | image | linkText | location | messageDeleted | report | spreadsheetPartition | text | user | video | voice",
  "messageSummary": "string",
  "messageTime": "string",
  "version": "string"
}
```

