import MasterChip from "../../../../../components/MasterChip"

# <MasterChip value={"DELETE"} /> messageRemoveForMe

## API Details

- **Description** Remove given messages for caller
- **Version** `v1`
- **Allowed Connection Types** `rpc` `wsoc` 
- **Allowed Deployment Kinds** `beta` `prod` `localHost` `test` 
- **Allowed Device Types** `test` `ios` `android` `web` `agent` 
- **SLA** 600
- **Token** bearer
- **Flags** Log message
- **Publishes** `messageRemoveForMe` 
- **Scope** globalOrEnt


## Request

### Message format

```json
{
  "offset": "number",
  "chatId": "ChatId"
}
```

### Signal format

```json
{}
```

