import MasterChip from "../../../../../components/MasterChip"

# <MasterChip value={"GET"} /> linkPreviewGet

## API Details

- **Description** Generate Link preview for the given link
- **Version** `v1`
- **Allowed Connection Types** `rpc` `wsoc` 
- **Allowed Deployment Kinds** `beta` `prod` `localHost` `test` 
- **Allowed Device Types** `test` `ios` `android` `web` `agent` 
- **SLA** 600
- **Token** bearer
- **Flags** Log message
- **Scope** global


## Request

### Message format

```json
{
  "url": "string"
}
```

### Signal format

```json
{
  "title": "string",
  "description?": "string",
  "imageUrl?": "string"
}
```

