import MasterChip from "../../../../../components/MasterChip"

# <MasterChip value={"DELETE"} /> chatRemove

## API Details

- **Description** Delete messages for given chat and also remove chat from recents
- **Version** `v1`
- **Allowed Connection Types** `rpc` `wsoc` 
- **Allowed Deployment Kinds** `beta` `prod` `localHost` `test` 
- **Allowed Device Types** `test` `ios` `android` `web` `agent` 
- **SLA** 600
- **Token** bearer
- **Flags** Log message
- **Publishes** `recent` `messageClear` 
- **Scope** globalOrEnt


## Request

### Message format

```json
{
  "chatId": "ChatId"
}
```

### Signal format

```json
{}
```

