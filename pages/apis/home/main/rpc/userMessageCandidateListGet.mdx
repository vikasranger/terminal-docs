import MasterChip from "../../../../../components/MasterChip"

# <MasterChip value={"GET"} /> userMessageCandidateListGet

## API Details

- **Description** Get user candidate list to send as user message
- **Version** `v1`
- **Allowed Connection Types** `rpc` `wsoc` 
- **Allowed Deployment Kinds** `beta` `prod` `localHost` `test` 
- **Allowed Device Types** `test` `ios` `android` `web` `agent` 
- **SLA** 600
- **Token** bearer
- **Flags** Log message
- **Scope** global


## Request

### Signal format

```json
{
  "candidateMap": {
    "AnyPrefixKey": [
      {
        "entId": "EntId",
        "entUserId": "EntUserId",
        "firstName": "string",
        "lastName": "string",
        "nickName?": "string",
        "userColor": "string",
        "handle?": "string",
        "about?": "string",
        "mediaIdAvatar?": "MediaIdAvatar",
        "isDeleted?": "boolean",
        "version": "string"
      }
    ]
  }
}
```

