import MasterChip from "../../../../../components/MasterChip"

# <MasterChip value={"GET"} /> groupCommonListGet

## API Details

- **Description** Gets common groups id list between caller and requested userId
- **Version** `v1`
- **Allowed Connection Types** `rpc` `wsoc` 
- **Allowed Deployment Kinds** `beta` `prod` `localHost` `test` 
- **Allowed Device Types** `test` `ios` `android` `web` `agent` 
- **SLA** 600
- **Token** bearer
- **Flags** Log message
- **Scope** global


## Request

### Message format

```json
{
  "entUserId": "EntUserId"
}
```

### Signal format

```json
{
  "groupIdList": [
    "GroupId"
  ]
}
```

