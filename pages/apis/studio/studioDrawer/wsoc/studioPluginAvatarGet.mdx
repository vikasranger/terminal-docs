import MasterChip from "../../../../../components/MasterChip"

# <MasterChip value={"GET"} /> studioPluginAvatarGet

## API Details

- **Description** Get Plugin avatar from draft or deployed plugin
- **Version** `v1`
- **Allowed Connection Types** `rpc` `wsoc` 
- **Allowed Deployment Kinds** `beta` `prod` `localHost` `test` 
- **Allowed Device Types** `test` `ios` `android` `web` `agent` 
- **SLA** 600
- **Token** bearer
- **Flags** Log message
- **Scope** plugin


## Request

### Message format

```json
{
  "version?": "string"
}
```

### Signal format

```json
{
  "pluginBundleId": "PluginBundleId",
  "name": "string",
  "about?": "string",
  "avatarId?": "MediaIdAvatar",
  "errorCount?": "number",
  "lastUpdateTime": "string",
  "isDirty?": "boolean",
  "hasLock?": "boolean",
  "version": "string"
}
```

