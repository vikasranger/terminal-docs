import MasterChip from "../../../../../components/MasterChip"

# <MasterChip value={"POST"} /> entAdminInvite

## API Details

- **Description** Send invite to admins / admins who has not joined yet to join
- **Version** `v1`
- **Allowed Connection Types** `rpc` 
- **Allowed Deployment Kinds** `beta` `prod` `localHost` `test` 
- **Allowed Device Types** `test` `ios` `android` `web` `agent` 
- **SLA** 600
- **Token** bearer
- **Flags** Log message
- **Scope** ent


## Request

### Message format

```json
{
  "adminIdSet?": [
    "AdminId"
  ],
  "inviteAllPendingAdmins?": "boolean"
}
```

### Signal format

```json
{}
```

