import MasterChip from "../../../../../components/MasterChip"

# <MasterChip value={"POST"} /> entUserAdd

## API Details

- **Description** Add user in enterprise address book
- **Version** `v1`
- **Allowed Connection Types** `rpc` 
- **Allowed Deployment Kinds** `beta` `prod` `localHost` `test` 
- **Allowed Device Types** `test` `ios` `android` `web` `agent` 
- **SLA** 600
- **Token** bearer
- **Flags** Log message
- **Publishes** `studioEntAddrBook` `studioEntFollowerList` 
- **Scope** ent


## Request

### Message format

```json
{
  "handle": "string",
  "nickName": "string",
  "roleIdSet?": [
    "MetaIdRole"
  ],
  "managerId?": "EntUserId",
  "follower?": "boolean",
  "userVariableValueMap?": {
    "MetaIdVar": "Object"
  },
  "entUserId": "EntUserId"
}
```

### Signal format

```json
{}
```

