import MasterChip from "../../../../../components/MasterChip"

# <MasterChip value={"GET"} /> entAdminEditLockDetailGet

## API Details

- **Description** Get admin details holding edit lock for enterprise
- **Version** `v1`
- **Allowed Connection Types** `rpc` 
- **Allowed Deployment Kinds** `beta` `prod` `localHost` `test` 
- **Allowed Device Types** `test` `ios` `android` `web` `agent` 
- **SLA** 600
- **Token** bearer
- **Flags** Log message
- **Scope** ent


## Request

### Message format

```json
{
  "version?": "string"
}
```

### Signal format

```json
{
  "adminId": "AdminId",
  "nickName": "string",
  "handle?": "string",
  "version": "string"
}
```

