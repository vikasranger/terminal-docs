import MasterChip from "../../../../../components/MasterChip"

# <MasterChip value={"POST"} /> pluginAdminAdd

## API Details

- **Description** Add admin for plugin
- **Version** `v1`
- **Allowed Connection Types** `rpc` 
- **Allowed Deployment Kinds** `beta` `prod` `localHost` `test` 
- **Allowed Device Types** `test` `ios` `android` `web` `agent` 
- **SLA** 600
- **Token** bearer
- **Flags** Log message
- **Publishes** `studioAdminBook` 
- **Scope** plugin


## Request

### Message format

```json
{
  "adminId": "AdminId",
  "handle": "string",
  "nickName": "string",
  "doNotShowOptionSet?": [
    "admins"
  ],
  "doNotEditOptionSet?": [
    "admins"
  ]
}
```

### Signal format

```json
{}
```

