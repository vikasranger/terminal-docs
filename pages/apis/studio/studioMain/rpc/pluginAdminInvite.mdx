import MasterChip from "../../../../../components/MasterChip"

# <MasterChip value={"POST"} /> pluginAdminInvite

## API Details

- **Description** Send invite to admins / admins who has not joined yet
- **Version** `v1`
- **Allowed Connection Types** `rpc` 
- **Allowed Deployment Kinds** `beta` `prod` `localHost` `test` 
- **Allowed Device Types** `test` `ios` `android` `web` `agent` 
- **SLA** 600
- **Token** bearer
- **Flags** Log message
- **Scope** plugin


## Request

### Message format

```json
{
  "adminIdSet?": [
    "AdminId"
  ],
  "inviteAllPendingAdmins?": "boolean"
}
```

### Signal format

```json
{}
```

