import MasterChip from "../../../../../components/MasterChip"

# <MasterChip value={"GET"} /> studioEntDeployStatusGet

## API Details

- **Description** Get enterprise deploy status
- **Version** `v1`
- **Allowed Connection Types** `rpc` `wsoc` 
- **Allowed Deployment Kinds** `beta` `prod` `localHost` `test` 
- **Allowed Device Types** `test` `ios` `android` `web` `agent` 
- **SLA** 600
- **Token** bearer
- **Flags** Log message
- **Scope** ent


## Request

### Message format

```json
{
  "version?": "string"
}
```

### Signal format

```json
{
  "entId": "EntId",
  "executionState": "created | initiated | inProgress | completed | failed",
  "lastUpdate": "string",
  "message": "string",
  "jobKey": "string",
  "version": "string"
}
```

