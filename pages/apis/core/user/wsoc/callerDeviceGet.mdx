# callerDeviceGet

## API Details

- **Description** Get caller device
- **Version** `v1`
- **Method** `get`
- **Allowed Connection Types** `rpc` `wsoc` 
- **Allowed Deployment Kinds** `beta` `prod` `localHost` `test` 
- **Allowed Device Types** `test` `ios` `android` `web` `agent` 
- **SLA** 600
- **Token** bearer
- **Flags** Log message
- **Scope** global


## Request

### Message format

```json
{
  "deviceId": "DeviceId",
  "version?": "string"
}
```

### Signal format

```json
{
  "deviceId": "DeviceId",
  "deviceName?": "string",
  "refreshTokenExpiry": "string",
  "creationTime": "string",
  "deviceToken?": "string",
  "randomColor": "string",
  "version": "string"
}
```

