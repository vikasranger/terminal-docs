import MasterChip from "../../../../../components/MasterChip"

# <MasterChip value={"PATCH"} /> forgotPassword

## API Details

- **Description** Forgot password sends OTP to the user and changes the password upon confirmation
- **Version** `v1`
- **Allowed Connection Types** `rpc` 
- **Allowed Deployment Kinds** `beta` `prod` `localHost` `test` 
- **Allowed Device Types** `test` `ios` `android` `web` `agent` 
- **SLA** 600
- **Token** none
- **Scope** global


## Request

### Message format

```json
{
  "newPassword": "string",
  "handle": "string"
}
```

### Signal format

```json
{
  "verifyKey": "AnyKey",
  "expiryMins": "number"
}
```

