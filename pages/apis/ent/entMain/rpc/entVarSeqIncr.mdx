import MasterChip from "../../../../../components/MasterChip"

# <MasterChip value={"GET"} /> entVarSeqIncr

## API Details

- **Description** Increment sequence value
- **Version** `v1`
- **Allowed Connection Types** `rpc` `wsoc` 
- **Allowed Deployment Kinds** `beta` `prod` `localHost` `test` 
- **Allowed Device Types** `test` `ios` `android` `web` `agent` 
- **SLA** 600
- **Token** bearer
- **Flags** Log message
- **Scope** ent


## Request

### Message format

```json
{
  "varName": "string"
}
```

### Signal format

```json
{
  "sequence": "number"
}
```

