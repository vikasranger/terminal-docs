import MasterChip from "../../../../../components/MasterChip"

# <MasterChip value={"GET"} /> entUserLastOnlineGet

## API Details

- **Description** Get status weather given user is online or not and last online time if available
- **Version** `v1`
- **Allowed Connection Types** `wsoc` `rpc` 
- **Allowed Deployment Kinds** `beta` `prod` `localHost` `test` 
- **Allowed Device Types** `test` `ios` `android` `web` `agent` 
- **SLA** 600
- **Token** bearer
- **Flags** Log message
- **Scope** ent


## Request

### Message format

```json
{
  "entUserId": "EntUserId"
}
```

### Signal format

```json
{
  "entId": "EntId",
  "entUserId": "EntUserId",
  "lastOnline?": "string",
  "online?": "boolean"
}
```

