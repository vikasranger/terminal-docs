import MasterChip from "../../../../../components/MasterChip"

# <MasterChip value={"GET"} /> spreadsheetRowCommentCountGet

## API Details

- **Description** Gets a form result
- **Version** `v1`
- **Allowed Connection Types** `rpc` `wsoc` 
- **Allowed Deployment Kinds** `beta` `prod` `localHost` `test` 
- **Allowed Device Types** `test` `ios` `android` `web` `agent` 
- **SLA** 600
- **Token** bearer
- **Flags** Log message
- **Scope** ent


## Request

### Message format

```json
{
  "formId": "MetaIdForm",
  "rowId": "RowId",
  "version?": "string"
}
```

### Signal format

```json
{
  "commentCount?": "number",
  "unreadCommentCount?": "number",
  "version": "string"
}
```

