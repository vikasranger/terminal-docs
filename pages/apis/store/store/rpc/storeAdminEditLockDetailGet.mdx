import MasterChip from "../../../../../components/MasterChip"

# <MasterChip value={"GET"} /> storeAdminEditLockDetailGet

## API Details

- **Description** Get admin details holding edit lock for store
- **Version** `v1`
- **Allowed Connection Types** `rpc` 
- **Allowed Deployment Kinds** `beta` `prod` `localHost` `test` 
- **Allowed Device Types** `test` `ios` `android` `web` `agent` 
- **SLA** 600
- **Token** bearer
- **Flags** Log message
- **Scope** store


## Request

### Message format

```json
{
  "version?": "string"
}
```

### Signal format

```json
{
  "adminId": "AdminId",
  "nickName": "string",
  "handle?": "string",
  "version": "string"
}
```

