import MasterChip from "../../../../../components/MasterChip"

# <MasterChip value={"PUT"} /> storeAdminUpdate

## API Details

- **Description** Update admin
- **Version** `v1`
- **Allowed Connection Types** `rpc` 
- **Allowed Deployment Kinds** `beta` `prod` `localHost` `test` 
- **Allowed Device Types** `test` `ios` `android` `web` `agent` 
- **SLA** 600
- **Token** bearer
- **Flags** Log message
- **Publishes** `studioAdminBook` `callerStudioAdmin` 
- **Scope** store


## Request

### Message format

```json
{
  "adminId": "AdminId",
  "handle?": "string",
  "nickName": "string",
  "doNotShowOptionSet?": [
    "admins"
  ],
  "doNotEditOptionSet?": [
    "admins"
  ]
}
```

### Signal format

```json
{}
```

