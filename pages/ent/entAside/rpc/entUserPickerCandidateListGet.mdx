import MasterChip from "../../../../../components/MasterChip"

# <MasterChip value={"GET"} /> entUserPickerCandidateListGet

## API Details

- **Description** Get list of user for given roles
- **Version** `v1`
- **Allowed Connection Types** `rpc` `wsoc` 
- **Allowed Deployment Kinds** `beta` `prod` `localHost` `test` 
- **Allowed Device Types** `test` `ios` `android` `web` `agent` 
- **SLA** 600
- **Token** bearer
- **Flags** Log message
- **Scope** globalOrEnt


## Request

### Message format

```json
{
  "roleIdSet?": [
    "MetaIdRole"
  ],
  "conjunction?": "or | and"
}
```

### Signal format

```json
{
  "candidateList": [
    {
      "entUserId": "EntUserId",
      "nickName": "string"
    }
  ]
}
```

