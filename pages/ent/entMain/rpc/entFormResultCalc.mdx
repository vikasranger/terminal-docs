import MasterChip from "../../../../../components/MasterChip"

# <MasterChip value={"POST"} /> entFormResultCalc

## API Details

- **Description** Executes formula and gets a new form result
- **Version** `v1`
- **Allowed Connection Types** `rpc` `wsoc` 
- **Allowed Deployment Kinds** `beta` `prod` `localHost` `test` 
- **Allowed Device Types** `test` `ios` `android` `web` `agent` 
- **SLA** 600
- **Token** bearer
- **Flags** Log message
- **Scope** ent


## Request

### Message format

```json
{
  "formId": "MetaIdForm",
  "formValueRaw": {
    "rowId": "RowId",
    "rowOrder?": "number",
    "createdOn?": "string",
    "createdBy?": "EntUserId",
    "updatedOn?": "string",
    "updatedBy?": "EntUserId",
    "valueMap": {
      "MetaIdComp": "Object"
    }
  }
}
```

### Signal format

```json
{
  "formValue": {
    "rowId": "RowId",
    "rowOrder?": "number",
    "createdOn?": "string",
    "createdBy?": "EntUserId",
    "updatedOn?": "string",
    "updatedBy?": "EntUserId",
    "valueMap": {
      "MetaIdComp": "Object"
    }
  }
}
```

