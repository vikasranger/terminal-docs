import MasterChip from "../../../../../components/MasterChip"

# <MasterChip value={"POST"} /> entPromptActionsGet

## API Details

- **Description** Return the matching prompt actions
- **Version** `v1`
- **Allowed Connection Types** `wsoc` `rpc` 
- **Allowed Deployment Kinds** `beta` `prod` `localHost` `test` 
- **Allowed Device Types** `test` `ios` `android` `web` `agent` 
- **SLA** 600
- **Token** bearer
- **Flags** Log message
- **Scope** ent


## Request

### Message format

```json
{
  "groupId": "GroupId",
  "promptText": "string"
}
```

### Signal format

```json
{
  "promptActionList?": [
    {
      "promptActionId": "MetaIdAction",
      "formValue": {
        "rowId": "RowId",
        "rowOrder?": "number",
        "createdOn?": "string",
        "createdBy?": "EntUserId",
        "updatedOn?": "string",
        "updatedBy?": "EntUserId",
        "valueMap": {
          "MetaIdComp": "Object"
        }
      }
    }
  ]
}
```

