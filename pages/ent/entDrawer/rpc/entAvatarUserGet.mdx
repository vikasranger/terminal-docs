import MasterChip from "../../../../../components/MasterChip"

# <MasterChip value={"GET"} /> entAvatarUserGet

## API Details

- **Description** Get enterprise avatar
- **Version** `v1`
- **Allowed Connection Types** `rpc` `wsoc` 
- **Allowed Deployment Kinds** `beta` `prod` `localHost` `test` 
- **Allowed Device Types** `test` `ios` `android` `web` `agent` 
- **SLA** 600
- **Token** bearer
- **Flags** Log message
- **Scope** global


## Request

### Message format

```json
{
  "entId": "EntId",
  "version?": "string"
}
```

### Signal format

```json
{
  "entId": "EntId",
  "name": "string",
  "about?": "string",
  "avatarId?": "MediaIdAvatar",
  "version": "string"
}
```

